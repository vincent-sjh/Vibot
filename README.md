# vibot

一个简单的 Python CLI 工具

## 安装

```bash
pip install -e .
```

## 使用

```bash
# 显示帮助信息
vibot -h

# 显示版本信息
vibot --version

# 测试功能 - 输出字符串两次
vibot -test "hello world"
```

## 开发

克隆项目后，在项目根目录运行：

```bash
pip install -e .
```

这将以开发模式安装 vibot，你可以直接修改代码并测试。